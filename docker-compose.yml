services:
  frontend:
    image: mayxu111/234-deploylab-frontend:latest
    pull_policy: always
    ports:
      - "8081:8080"
    environment:
      - VITE_GRAPHQL_URI=http://[Your_IP_or_Hostname]/graphql
      - VITE_SERVER_URI=http://[Your_IP_or_Hostname]:8082
    restart: unless-stopped
