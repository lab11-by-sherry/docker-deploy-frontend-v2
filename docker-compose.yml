services:
  frontend:
    image: mayxu111/234-deploylab-frontend:latest
    pull_policy: always
    ports:
      - "8081:8080"
    environment:
      # 把这里换成你的实际IP
      - VITE_GRAPHQL_URI=http://100.29.7.182:8082/graphql
      - VITE_SERVER_URI=http://100.29.7.182:8082
    restart: unless-stopped
